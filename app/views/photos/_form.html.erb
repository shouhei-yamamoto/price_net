<%= form_with(model: photo, local: true) do |f| %>
  <% if photo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(photo.errors.count, "error") %> prohibited this photo from being saved:</h2>

      <ul>
        <% photo.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :image %>
    <%= image_tag(photo.image.url) if photo.image && photo.image.url %>
    <%= f.file_field :image %>
    <%= f.hidden_field :image_cache %>
  </div>

  <div class="content">
    <%= f.label :'写真説明' %>
    <%= f.text_field :content %>
  </div>

  <%= f.fields_for :products do |product| %>
    <p><%= render 'product_fields', f: product %></p>
  <% end %>

  <div class = 'link' >
    <%= link_to_add_association '金額欄追加', f, :products %>
  </div>

  <%= f.fields_for :product_urls do |product_url| %>
    <p><%= render 'product_url_fields', f: product_url %></p>
  <% end %>

  
  <div class = 'link' >
    <%= link_to_add_association 'URL欄追加', f, :product_urls %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
